export const viewWidth = 1800;
export const viewHeight = 1800;

export const outerMargin = 300;
export const innerMargin = outerMargin + 75;

export const pageWidth = viewWidth - (2 * outerMargin);
export const textAreaWidth = viewWidth - (2 * innerMargin);

export const pageHeight = viewHeight * (2 / 3);

export const fontSize = 24;